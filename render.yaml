services:
  - type: web
    name: singer-mashup-generator
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: SENDER_EMAIL
        sync: false
      - key: EMAIL_PASSWORD
        sync: false